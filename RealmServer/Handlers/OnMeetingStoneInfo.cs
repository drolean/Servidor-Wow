namespace RealmServer.Handlers
{
    internal class OnMeetingStoneInfo
    {
        public static void Handler(RealmServerSession session, byte[] data)
        {
            // session.User

            /*
	data.Initialize( SMSG_MEETING_STONE_INFO );
	data << uint32(0);
	data << uint32(0);
	data << uint32(0);
             */
        }
    }
}
